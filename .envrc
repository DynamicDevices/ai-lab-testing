# Use Python 3.10 for this project
# Requires direnv: https://direnv.net/
# Run: direnv allow

# Prioritize system Python 3.10 over anaconda3
export PATH="/usr/bin:$PATH"

# Verify Python version
python3 --version

